-# Simple layout
-# one column, readable width, no fanciness

= wrap_layout :layout do
  .one-column
    = yield

  = javascript_include_tag "//cdn.jsdelivr.net/npm/anchor-js/anchor.min.js"
  :javascript
    anchors.add();
